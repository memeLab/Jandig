import { Header } from "../header/Header";
import { UseGuide } from "../use_guide/UseGuide";

export const Home = (props) => {
  return (
    <div>
      <Header />
      <UseGuide />
    </div>
  );
};
