{% extends '/pwa/base.jinja2' %}

{% block content %}

    <a-scene embedded arjs>
        
        {% for artwork in artworks %}
            {% if artwork.patt == "hiro" %}
                <a-marker type="pattern" preset="{{artwork.patt}}">
                     <a-entity id="{{artwork.patt}}-gif" gif="src: static/images/{{artwork.gif}}.gif;
                                                            marker: #{{artwork.patt}};
                                                            scale: {{artwork.scale}};
                                                            rotation: {{artwork.rotation}};
                                                            position: {{artwork.position}};">
                    </a-entity>
                </a-marker>
            {% else %}
                <a-marker id="{{artwork.patt}}" type="pattern" url="static/patts/{{artwork.patt}}.patt">
                    <a-entity id="{{artwork.patt}}-gif" gif="src: static/images/{{artwork.gif}}.gif;
                                                            marker: #{{artwork.patt}};
                                                            scale: {{artwork.scale}};
                                                            rotation: {{artwork.rotation}};
                                                            position: {{artwork.position}};">
                    </a-entity>
                </a-marker>
            {% endif %}
        {% endfor %}

        <a-entity camera></a-entity>         
    </a-scene>

{% endblock %}