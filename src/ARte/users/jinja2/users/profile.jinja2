{% extends '/core/home.jinja2' %}

{% block content %}
    {% block subcontent %}
    {% endblock %}
    <section class="profile-options-flex">

        {# FIXME: maybe this can be improved #}
        <link rel="stylesheet" href="/static/css/profile.css">
        
        <a class="option-link" href="edit/">{{ _('Edit personal info') }}</a>
        <a class="option-link" href="{{ url('marker-upload') }}">{{ _('Upload marker') }}</a>
        <a class="option-link" href="{{ url('object-upload') }}">{{ _('Upload object') }}</a>
        <a class="option-link" href="{{ url('artwork-creation') }}">{{ _('Create Jandig artwork') }}</a>
        <a class="option-link" href="{{ url('exhibit-create') }}">{{ _('Create exhibit') }}</a>

        <section class="account-options">
            <a href="{{ url('logout') }}"> {{ _('Log out') }} | </a>
            <a href="#"> {{ _('Remove account') }}</a>
        </section>
    </section>
{% endblock %}