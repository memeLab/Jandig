{% extends '/core/arviewer.jinja2' %}

{% block content %}
    <link rel="stylesheet" href="/static/css/signup.css">
    <div class="connect-modal container">
        <div class="logo">
            <a href="{{ url('home') }}">
                    <img src="/static/images/icons/header_icon.png">
            </a>
        </div>

        <div class="modal-container">
            <div class="modalMenu flex">
                <span>{{ _('Your verification code is invalid.') }}</span>
            </div>

            <a href="{{ url('home') }}">
                <span>{{_('Click here to return')}}</span>
            </a>
        </div>
    </div>
{% endblock %}